# snowflake-ai-sql-audio-parsing
This project demonstrates how to use Snowflake AI_SQL Cortex functions to transcribe audio, mine unstructured data, translate text, redact sensitive content, and compare image embeddings — all inside the Snowflake Notebook UI.
